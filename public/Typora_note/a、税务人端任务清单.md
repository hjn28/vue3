---

---

### 0406

```js
1、功能侧边栏的菜单，取session当中的数据，用来显示和隐藏
2、首页当中的待办事宜的金三未显示数据  
	空白页的处理
3、sword项目下拉树的测试与添加，提交组件传递的数据，进行页面的铺开

4、vscode代码提交记录git查询插件

5、功能验收的情况

格林德酒店 公寓式酒店
3个标间 220 不含早餐 可以对公
```

### 0411

```js
安装数据库 进行数据库的链接
底部数据的统计完成
8点半 开始学习小程序
```

我怕风浪大？风浪越大，鱼越贵。

### 任务列表清单

```js
# 按照这个清单 样式写
1、
问题描述：预生产总局待办事宜的数据未展示问题
造成原因：返回的数据当中有部分数据未传输过来，未对其进行判空处理
解决方案：增加对返回数据是否存在的判断
2、
问题描述：预生产省局调用关闭当前tab页的方法，出现不能调用的情况
造成原因：其使用者未做跨域处理
解决方案：让使用者增加跨域处理
```



### 0517

```js
修复门户省局预生产金三页面点击“文书送达”报错，能够正常打开文书送达办理页面；
修复门户总局预生产功能菜单点击无法展示子菜单漏改bug修复；
修复门户总局预生产个人待办任务，岗位待办任务，在办任务，界面显示和小标不一致
修复待办事宜日期控件可以选择具体日期时分秒，显示到时分秒；列表信息展示岗位名称一列不显示；查询条件纳税人识别号名称完整显示；
# 已发送任务修改记录 发布
1、
【问题简述】门户省局预生产金三页面点击“文书送达”报错；
【造成原因】传递的参数，后端没有接收到；
【解决方案】 门户省局预生产金三页面点击“文书送达”，修改传递的参数
2、
【问题简述】门户总局预生产功能菜单点击无法展示子菜单；
【造成原因】总局接口返回的数据，字段为null的没有显示出来，导致找不到“children”字段；
【解决方案】 门户总局预生产功能菜单接收到的数据增加“children”字段；
3、
【问题简述】门户总局预生产个人待办任务，岗位待办任务，在办任务，界面显示和小标都不一致；
【造成原因】关闭其他选项卡，首页未做重新加载待办事宜的数据；
【解决方案】 门户总局预生产首页选项卡进行关闭当前选项，在首页按钮上进行关闭其它选项和恢复默认首页这三处触发待办事宜的请求接口；
4、
【问题简述】待办事宜当中日期组件不显示时分秒，选项的名称显示不完整，岗位名称一列没有数据；
【造成原因】日期展示宽度不够，导致时分秒展示不全，未展示；选项的名称做了字段长度的判断，超过长度做了隐藏，岗位名称一列没有数据，展示的是空数据；
【解决方案】 待办事宜当中选项栏一排显示三个，日期组件显示时分秒，选项的名称取消对字段的隐藏，岗位名称没有数据不显示这一列；
```

要发送的任务列表信息

```js
1、
【问题简述】门户省局预生产金三页面点击“文书送达”报错；
【调整内容】 门户省局预生产金三页面点击“文书送达”，修改传递的参数
【期望结果】门户省局预生产金三页面点击“文书送达”，能够正常打开文书送达办理页面。
2、
【问题简述】门户总局预生产功能菜单点击无法展示子菜单；
【调整内容】 门户总局预生产功能菜单接收到的数据增加“children”字段；
【期望结果】门户总局预生产功能菜单点击可以正常展示子菜单；
3、
【问题简述】门户总局预生产个人待办任务，岗位待办任务，在办任务，界面显示和小标都不一致；
【调整内容】 门户总局预生产首页选项卡进行关闭当前选项，在首页按钮上进行关闭其它选项和恢复默认首页这三处触发待办事宜的请求接口；
【期望结果】门户总局预生产个人待办任务，岗位待办任务，在办任务，界面显示和小标显示一致；
4、
【问题简述】待办事宜当中日期组件不显示时分秒，选项的名称显示不完整，岗位名称一列没有数据；
【调整内容】 待办事宜当中选项栏一排显示三个，日期组件显示时分秒，选项的名称取消对字段的隐藏，岗位名称没有数据不显示这一列；
【期望结果】待办事宜当中日期组件显示时分秒，选项的名称显示完整，岗位名称没有数据不显示这一列；
```

### 待完成的任务或将要进行的任务汇总

```js

```

### 0911

```js
vue-grid-layout 的使用
熟悉下使用方式，研究源码是如何实现的
元素属性

```

### 智慧办公平台

```js
注意点:
	1、颜色的统一设定；（导航栏的颜色设置）
    2、字体的大小设置，分为大中小来走；
    3、卡片的拖拽和展示全部在一个组件当中； 涉及到 卡片的存放位置，怎么来存储；
    

    自定义工作台和首页实际走的是一套公共组件。所以卡片的拖拽啥的，动态添加，以及页面的加载做成统一一个公共组件
    
```



```js
针对接收到的数据 进行处理判断，有哪些需要注意的点
整合css 颜色 字体大小的设置；

判断的数据 对于数字为0 的时候，if 判断是false；


针对自定义工作台：
实现思路：

git执行 commit 时，进行crlf 切换为 lf操作；

新电局当中待办任务
判断标题如果带 “开具收入退还书（出口退税）”的话，拼接一个cktsdbbz=Y
&cktsdbbz=Y

tabComplete(row, flag) {
  const obj = {
    id: flag ? 'clickTaskTheme' : 'clickTaskTheme1',
    title: row.rwzt,
    src: row.rwbldz,
    tempData: row.rwzt,
  };
  window.top.addTabItem(obj);
},
    
关于grid当中的宽度设置
grid当中的网格按照6*12的比例
网格列数：col-num = 6
最大行数：maxRows = 12
边距设置：margin=[12,12]
每个格子的高度：42px
卡片的宽度高度设定为 w:208px; y:52px;


卡片 1328
208 6 = 1248 
5*16 = 80


接下来的工作任务：
1、进入自定义工作台时，界面是填充的工作台当中的数据；在自定义工作台操作完成之后，点击完成按钮，将自定义工作台当中的数据（这里的数据暂时可以存在本地），渲染到工作台当中；
2、界面优化：内容区域的环形

三个任务：
1、进入自定义工作台时，界面需要显示工作台当中的数据；在自定义工作台操作完成之后，点击完成按钮，将自定义工作台当中的数据（这里的数据暂时可以存在本地），渲染到工作台当中；

2、绘制界面：消息主界面
		通过点击左侧消息展开；
3、绘制界面：日程主界面、日程修改
		通过点击左侧日程展开
        
今日任务：
优化界面展示，弹窗改为浮动界面
```

### 新电局

```js

```

### 0928

```js
1、页面进行简要的优化 包含图形的重叠生成的线条；
2、理一下现有的代码 实现，如何对卡片进行存储；
3、关于卡片添加的时候，对区域的判断；
4、进行拖拽的时候，如何保证区域不超过边界

5、添加界面的修改，添加界面有一个透明度的底色，选中恢复透明度，右上角显示勾选；

依赖的清理与删减
```



### 国庆节后安排

```js
天津环境测试 消息查询 消息个数展示

账号
11245200023
密码
Abcd1234


```



### 1006

```js
任务需求：
1、待办任务中，办结期限、发送时间0进行排序，存在默认排序；
2、待办任务界面 进行界面左右抽屉实现；
3、已有的接口接入，可以写好实现的逻辑；有接口的时候直接引入；
```

### 工作进度

```js
1、收藏夹功能界面
2、取消头部搜索框
3、在首页当中添加右侧三种浮标，分别为：收藏夹、点击调用除了首页之外的tab页当中的方法（类似于原首页调用的刷新功能）、回到顶部（暂时先不做）；
	优先处理两个 收藏夹、调用方法
    点击收藏夹浮标，前置条件需要优先将打开的tab页当中的id、名称一些相关信息存储起来；当点击右侧收藏浮标的时候，可以根据高亮效果，去匹配对应的数据，调后端接口存数据；
    点击调用方法，一样的也是区分页面，如何识别对应的方法；
4、增加首页tab选项卡格式；
5、关于消息界面展示 到时候调用消息中心的来展示；
6、代码整合到门户局端上；

7、头部导航栏 左右tab选项卡 选择；刷新支持页面刷新；
8、收藏夹功能 采用弹框或者是抽屉的形式展示，从最右侧 向左展开；

9、工作时间提醒，页面加载时间计算，根据用户打开界面进行提醒，超过2小时提醒一次；这个时间是存储在localstorage、还是sessionStorage 合适；额外增加的：当用户登录超时时，时间重新计算；

10、选项卡设置首页，首页默认展示是工作台；点击左侧功能，替换首页加载点击的某一项，名称改为点击的功能名称；左侧的收藏夹是浮框的形式展开，不在首页那展示；

11、表格的高度统一设定，在充满卡片的时候，正确展示；熟悉一下tab标签的数据展示与数据的存储；进行规划表格的大小的值，统一起来；将公共的参数值存在一起；

12、将现有的接口对接起来，处理登录、退出跳转，密码修改
13、右侧的浮标
14、less支持换肤功能，点击换肤，选择换肤的样式，支持换肤；难点在于：如何去修改less文件当中的值；

15、tab选项卡 增加最小宽度	√
16、路由匹配当前的选项卡


```





```js
时间截点：
页面更改样式 3d
业务界面 3d

三个浮框；收藏、调用方法、置顶；2d
路由的切换方式、支持iframe样式的跳转；3d
支持换肤功能 1d

```



### 后期需要的工作项

```js
#
1：在首页当中添加右侧三种浮标，分别为：收藏夹、点击调用除了首页之外的tab页当中的方法（类似于原首页调用的刷新功能）、回到顶部（暂时先不做）；
	优先处理两个 收藏夹、调用方法
    点击收藏夹浮标，前置条件需要优先将打开的tab页当中的id、名称一些相关信息存储起来；当点击右侧收藏浮标的时候，可以根据高亮效果，去匹配对应的数据，调后端接口存数据；
    点击调用方法，一样的也是区分页面，如何识别对应的方法；
#
2：政务微信唤起
	scheme方式唤起	文件所在目录	D:\hujianeng\202310	8+第三方APP或web如何唤起企业微信或政务微信App(1).pdf
3：2.0版跳转1.0时，需要携带参数，将导航栏的大标题更改；

4、消息中心界面 只展示通知公告，前端给你们传递系统参数，进行区分两个不同的系统；	10月17号和完备进行沟通
```

### 当天需要完成的任务

```js
功能的跳转，选项卡左右切换，首页实现左右路由切换；
需要套用iframe选项卡来实现跳转；
在跳转的时候传递参数，进行区分是否需要排在第一个；

在进行左右切换的时候，如何去触发点击事件，展示该页面？
左右切换的时候，根据当前读取的value或者id来进行区分，以及是否可点，进行判断；
点击左侧的功能选项直接去触发tab首页的标签；

待处理的问题：
一个是新增的tab页的显示，共用iframe，名称未显示出来，
删除tab页
刷新操作
iframe适配宽度 已适配
tab添加新的标签 左侧选项卡更新；

使用$emit去触发tab的change事件，但是已加载后的界面不会再重新加载；

最近访问

待办数据来源 改成 电子税务局
刷新的时候重新请求地址去刷新页面；

需要后端更改的方法：
登录接口方法  mhloginZhbgpt/init
登录首页地址	zhbgpt

tab标签刷新问题
添加新功能
系统名称对应的是发送来源 xtmc 字段
1020
用户行为记录接口，通过点击功能时触发，
初次加载的时候，常用功能接口需要一个每隔五分钟的触发；能及时更改常用功能的数据；
保存自定义工作台，
自定义的添加；
刷新当前界面效果，
退出接口；
登录接口；

```



### 这两天需要处理的任务

```js
17号下午和王蓓进行沟通 商量这个事
消息中心界面 只展示通知公告，前端给你们传递系统参数，进行区分两个不同的系统；	10月17号和完备进行沟通
```

### 完成的任务

```js
1、已完成的任务：
界面绘制（已完成）：
	左侧导航栏、头部导航栏、工作台、自定义工作台、消息、我的功能界面；支持自定义工作台进行拖拽；
接口对接：
	我的功能当中接口已对接（包含获取数据接口、新建分类、修改文件夹名字、常用功能删除、置顶功能、查询接口）；工作台接口（初始化工作台数据接口）；最近访问获取已完成；工作台自定义保存接口；
2、目前待完成任务：
	界面：工作台大厅界面；新增的卡片未渲染；
    接口: 工作台保存接口；添加卡片接口；用户行为记录，最近访问删除；最近访问添加；登录接口；退出接口；消息接口；待办接口；通知公告接口；
    方法：tab刷新高亮标签方法；点击功能跳转方法；
    自定义工作台的拖拽越边界问题待解决；
3、集成环境暂时没有推；

登录退出 自定义工作台 、
消息展示进行倒序；

跳转方法 提供出来

1、待办任务数据 对接
	系统名称对应的是发送来源 xtmc 字段
2、tab页跳转 tab页标签的字体样式 隐藏
3、工作台直接添加的卡片形状有偏差

4、功能项的点击星星收藏 与 点击功能项跳转区分开；
5、其他可以一直都在
6、tab标签关闭功能


后期常用功能查询分级别，查询；第一层分类查询，

4、用户行为记录、最近访问


1、待办任务数据 对接
	系统名称对应的是发送来源 xtmc 字段
2、tab页跳转 tab页标签的字体样式 隐藏
3、工作台直接添加的卡片形状有偏差

4、功能项的点击星星收藏 与 点击功能项跳转区分开；
5、其他可以一直都在
6、tab标签关闭功能
7、大卡片替换 图片不存在，会报错，形成阻断
8、自定义工作台点击保存时，点击添加卡片时，再触发添加卡片接口
```

### 1026

```js
1、工作台直接添加的卡片形状有偏差
2、大卡片替换 图片不存在，会报错，形成阻断
3、
任务大卡片出现两张重复的 有先前的样式

政务微信

x
简易处罚 放在倒数第二个
打开新的tab标签 iframe地址
退出 点击退出

tab高亮效果、 tab页面渲染效果


两个问题：
一个是header当中给的名称只显示前两个字，或最后两个字
两行加隐藏

里一个是打开新的iframe标签，铺的界面分开

左侧sider导航栏 与第一个标签匹配；

当前问题：
1、差一个tab地址处理 与高亮效果 iframe页面

2、点击退出登录 界面会出现左侧移动
3、头部身份显示 超过两行 隐藏 overflow:hidden;
4、点击刷新 按钮 就行刷新本页

5、常用功能的自动添加 每隔五分钟触发一次


自定义工作台

1024
头部导航栏超过两行的处理 86账号
刷新当前页面，通过接口来判断是否进行刷新
更改图标 刷新图标、消息界面图标 左侧导航栏图标
iframe嵌入的页面的高度适配 防止出现滚动条


1、消息待办当中的数字显示与待办数据的接口保持一致； t
2、消息中的待办、待办任务领导岗的右侧滚动条调整为小滚动条； t
3、常用功能的文件夹下钻，功能点击调用window.top.addTabItem2跳转 t
4、功能选项展示效果图完善 u
5、常用功能卡片、业务卡片，当内容一页的情况下，不显示左右切换和下方的点；两页以上，鼠标移入当前卡片展示左右切换和点，鼠标移出卡片，隐藏左右切换和点； u
6、待办任务当中基层岗，办结期限点开，选择项是框；t

1、调整常用功能当中的数据展示效果，区分三种展示效果：
	（1）、无分类	数据平铺展示
    （2）、有分类	数据都在分类中，无其他
    （3）、有分类、有其他
    业务功能点击收藏时，三种情况：
    1、无分类时，点击直接收藏到常用功能
    2、有分类时，显示分类与其他
2、功能选项效果展示图完善


1、iframe嵌入的页面的高度适配 防止出现滚动条 u
2、刷新当前页面，通过接口来判断是否进行刷新 u
3、打开新的功能选项时，左右选择的状态修改 t
4、左侧点击之后 高亮效果的显示 t
5、当进入税务人智慧办公平台 页头的title也要发生改变 t
6、调整调用方法 通过window.top.addTabItem 传参就行改变 u
7、tab页展示的样式修改 差一个新添加的时候，图标的展示判断； t


刷新当前页分两种情况：
	1、作为首页替换页
    2、作为功能tab标签，存在在数组当中的数据
    思路是：点击刷新按钮替换当前的地址，但由于tab标签不会重新渲染，可以考虑替换iframe地址的方式；
    
    sider高亮效果 消息侧边栏高亮效果 t
    
卡片的左右选项及点点点的显示：
    1、只有一页数据的情况，不显示左右选项及点点点；
    2、两页及以上，显示点点点，左右选项根据移入移出，做显隐；
    
sider当中的 高亮效果 有左右间距；t
iframe界面样式修改 u

10月26号完成

待办领导岗 点击 t
常用功能没有查看全部 t
常用功能的点击跳转，常用功能下钻；
加汁抵减税 有问题 t

图片渲染处理问题；


tabbar.vue 文件 是做什么用的？

身份切换 对接一下放在哪个位置；
刷新触发；

点击时 增值税申报 可以打开界面

10月26号

点击收藏的时候，搜索会触发，展示星星显示不正确；t


添加最近访问接口，删除最近访问，
在右侧当中增加身份切换；

加汁抵减税 有问题
swap

```

### 1027

```js
页面刷新问题
	1、页面刷新采用的是 左侧 点击之后拿到的id，取数组当中取数据，调用addTabItem2方法，携带参数 isfresh参数 根据参数为true的时候，进行src='',再使用src= obj.src;实现页面的刷新操作 去调用
	2、如果点击的是功能的id 去取当前id的数据 使用isfresh去刷新功能是通过id在数组当中取对象，然后携带参数调用方法
    
    刷新事件可以使用，还需要考虑的是 打开同个功能，判断id进行处理；
    调用事件 同时传递三个参数；
    
    查看一下 get post请求的传参问题 query
    梳理一下点击功能跳转的方法；
    mergeInterface 每次刷新都会触发刷新；
    
    通过点击功能打开的界面 给一个padding：20px；
    
修改调用方法 window.addTabItem2
用户行为记录 传递参数更改；

核对一下 content中的兼容金三的方法，以及跨系统调用的方式
尝试动态修改less文件当中

跨系统调用对接
window.addTabItem 根据打开方式 dkfs 去进行打开新的功能

```

### 1030 跨系统调用、新跨系统调用

```js
文件存放路径：D:\hujianeng\202310\待处理的跨系统调用方式js
跨系统调用：
	
新跨系统调用：
	1、需要后端提供一下 swrysfdm 
    2、往后端发送请求的时候，采用的post请求，参数不在url上展示；原先使用的传递参数是form表单的形式；
        PageContainer.implement({
        	SwordformSubmit:function (req, target) {
                var form = new Element('form', {
                    'name':"swordDownLoadForm",
                    'method':'post',
                    'target':target,
                    'action':url,
                    styles:{'display':'none'}
                }).inject($(document.body));
                new Element('input', {
                    'name':"token"
                }).inject(form).set('value',srice);
                form.submit();
            }
        });
	3、
```



### 1030 页面调整

```js
栅格 12* 12  原有的3*4 改为 6*4
左侧边栏 64px 选中样式背景色 填满
tab页标签 36px
tab标签 字体 增加模糊处理 谷歌浏览器标签
1440* 900
工作台间距 20px
全屏状态

功能点击的时候的跳转，左右选择时出现不能往返选择；	1
常用功能 
业务功能当中的选项 移入之后边框和星星同时展示，增加收藏删除操作	1
点击功能选项时，要跳转到对应的tab标签；	1
点击自定义工作台保存时，需要sider回到左侧工作台；	1

JSON.string
JSON.parse

点击保存之后 如何去触发sider的高亮效果	1
需要在sider写一个方法，通过保存之后，	1

1、项目启动之后，自动触发全屏模式
2、目前所有的宽 全乘以2，高度是通过自适应 算出来的；


自定义工作台数据保存的时候，有一个初始化模板，可供使用，

window.addTabItem()方法调用
左右切换选项，通过tabList 去进行操作；
方法重写;
```

### 新功能

```js
多人协作处理表格；
```



### 最重要的事情

``` js
对于数据的判断 数据判断的时候，

将卡片的链接 输出给后端；采用的是接口返回的；

组件的链接 采用是后端获取数据 进行渲染
卡片的添加


1、本地环境 无法加载 vue-grid-layout ，查看ie11 兼容的版本 
2、集成环境 类似是crypto-js 造成阻塞，切换版本3 尝试



neng17836920840#

TIconReplaceWebpackPlugin is not function or its return value is not iterable

查看浏览器版本问题：
keyuse

消息中心 数据测试
消息中心 未读消息个数的计算；针对数据的个数的判断

点击未读消息 进行数据刷新；

进行新跨系统调用测试；

常用功能 10个icon
```



```js
待办任务大卡片 当中的 期限后边的图标；时间后边的上下箭头；
工作台卡片位置偏差；
工作台计算高度 计算有问题；

1、对出现的bug进行维护
2、接着开发进度
3、日程开发、重要记事卡片相关
4、全屏展示，界面效果改为12*12
5、卡片当中的数据 根据当前卡片的宽度进行摆放个数，计算出当前页可摆放的功能个数；涉及到的左右切换按钮，提供节流、防抖的公共方法；
6、大厅卡片的排版，按照1920的大屏宽度，进行排版；在1440的屏上，宽度保持不变，可以实现左右移动；通过transform translate进行左右移动；
7、卡片的图片地址存储，与读取；涉及到的iframe通过地址去读取卡片的路径，实现卡片的展示；
8、人文关怀提示，在下方进行滚动条展示；
9、卡片的模板维护界面；
10、预设模板维护界面
11、消息界面变动
12、总体布局发生改变


目前遗留的问题：
	跨系统调用；
    新跨系统调用
    点击跳转的方法 打开新的tab页 方法 进行优化；
    
    自定义工作台添加卡片页面加载当中存在假数据
    https://docs.qq.com/sheet/DZEVBTWdJTlpLVG1x?tab=BB08J2&u=6d4df9e68a7c4db58e73f0b524b4e70b

数据库 图片存为byte数组，前端如何解析
公共方法：对图片进行处理；base64 ；
iframe 自定义工作台无法进行拖拽；

日期组件当中涉及到的接口：
模板卡片界面
优化tab跳转方法


1、页面打开自动刚进入全屏模式；
2、frame嵌套卡片 无法进行拖拽；
3、添加卡片界面优化，图片读取通过后端返回字段进行base64处理，展示；
4、大厅展示卡片个数，使用transfrom进行处理；


1、
```

### 11月13日

```js
1、准备个版本出来 发到陕西预生产，其他的不动；预计14日发版本；√
2、确认工作台数据：12*12；卡片存储图片；√
3、消息界面修改（简易修改）；√
4、自定义工作台界面修改；
5、iframe 渲染界面；
6、添加卡片采用缩略图的效果展示；√

7、测试案例；下午书写；

```

### 配置组

```js
要tec账号密码 登录陕西预生产 查看相关配置
```



### 可信要账号

```js
管理所的科长、科员在实际环境中是没有征管+纳服条线的；
修改管理所所长和管理所科员的 条线：修改为  基础管理条线+风险应对条线

```

![](C:\Users\HJN\AppData\Roaming\Typora\typora-user-images\image-20231114155437544.png)

```js


```



### 身份切换问题

```js

```

### 配置文件的修改

```js
配置文件的修改：mh-ctrl-jd-web
下周一 申请清单 发布;

```

### 今日任务

```js
提供消息点击未读变已读的方法；

优化 点击跳转方法

区分目前的2.0 调用的金三是有问题的，原先的1.0支持；目前2.0策略不通；
```

### 遗留的问题

```js
1、消息提醒的高度，需要修改iframe高度，值修改为动态设置 自定义工作台 加遮罩层，
	使用iframe，造成高度不一致，需要调整；感觉是boder造成的影响；
2、tab标签
3、兼容IE调整大小
4、卡片采用iframe 样式嵌套
5、自定义工作台加载 数据不走本地，直接走接口请求；本地容易造成遗留；
6、卡片高度不应该设置固定高；待办12*4、常用功能、业务待办；
7、本地样式与集成样式不一致；业务卡片和常用功能卡片；
8、待办任务颜色 缺少渐变色
9、卡片涉及到数据为空时的样式变化；


19日调整：
1、tab标签 页头样式调整
2、左侧sider展开样式
3、图片上传按钮

快捷链接 是弹出新窗口 window.open()

常用功能渲染有问题，

过一下 使用的跳转方法，window.addTabItem2 使用的场景 及传递的参数；
图片上传与删除；


出一版新的tab调用方法
```

### 

```js
常用功能

原先的发版流程是，集成成构建好，将需要出厂的信息发给配置组；


```



```js
1、我的功能当中的分类 置顶 展示
2、
```

### 卡片进行轮播图

```js
1、横向的柱状图卡片数据进行上下 轮播；鼠标移入当前的页面，轮播停止；鼠标自动滑动；
2、功能跳转时 同张卡片 进行功能跳转，加类似预设模板的标题；
3、
```



```js
周报使用：
处理跨系统调用的模态弹框无法弹出问题；
修改待办任务卡片界面；
更换新一版卡片涉及到的icon图标；
```

### 发版记录描述

```js
发版预生产修改描述：
1、工作台界面待办任务卡片、业务功能卡片、场景办税卡片图标样式修改；
2、重要记事卡片重做；
3、首页tab标签的样式修改；
4、我的功能界面当中的快捷链接、业务功能图标更改；
5、业务功能卡片点击查看全部的业务功能界面的图标更改；
6、日程界面背景修改，添加‘今’字图标；


mh-front-jd-web：master-20240202-174918
mh-front-jd-web：master-20240218-170301
```

### 会议纪要

```js

```

